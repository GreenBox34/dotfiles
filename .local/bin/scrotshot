#!/bin/sh
# -*- mode: sh; sh-basic-offset: 2; -*-

DIR="${HOME}/Pictures/Screenshots/"
FILE_NAME="%Y-%m-%dT%H:%M:%S-screenshot.png"
FLAGS="--freeze --quality 100 --select"

mkdir -p "${DIR}"
scrot ${FLAGS} "${DIR}${FILE_NAME}" 

notify-send "scrotshot" "Screenshot taken"
